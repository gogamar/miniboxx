<div class="group relative">
  <div class="aspect-h-1 aspect-w-1 w-full overflow-hidden rounded-md bg-gray-200 lg:aspect-none group-hover:opacity-75 lg:h-80">
    <a href="<%= variant_path(variant) %>">
      <img src="<%= variant.image_urls.first.url %>" alt="<%= variant.product.short_description %>" class="h-full w-full object-contain object-center lg:h-full lg:w-full">
    </a>
  </div>
  <div class="mt-4 flex justify-between">
    <div>
      <h3 class="text-sm text-gray-700">
        <a href="<%= variant_path(variant) %>">
          <span aria-hidden="true" class="absolute inset-0"></span>
          <%= variant.product.short_description %>
        </a>
      </h3>
      <p class="mt-1 text-sm text-gray-500"><%= variant.color.name %></p>
    </div>
    <p class="text-sm font-medium text-gray-900"><%= variant.product.rrp_value %>€</p>
  </div>
</div>
